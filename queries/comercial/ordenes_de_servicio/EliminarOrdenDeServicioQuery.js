const { query } = require('../../../config/db');

const EliminarOrdenDeServicioQuery = async () => {
    const sql = `

    `;
    return query(sql);
};
module.exports = {
    EliminarOrdenDeServicioQuery
};